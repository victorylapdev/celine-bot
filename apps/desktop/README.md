# Desktop client

Reserved for the desktop UI. Choose PySide6 for a Python-native first version,
or Tauri + React when a richer web-based interface and smaller native shell are
the priority. The client communicates with the backend over WebSocket and HTTP.
